<scroll-view class="scrollarea" scroll-y type="list">
  <view class="container">
    <view class="title">
      <text class="title-text">浓度预测</text>
    </view>
    <view class="card">
      <text class="label">图片：</text>
      <button class="button" bindtap="chooseImage">拍照/选择图片</button>
    </view>
    <view class="card" wx:if="{{imagePath}}">
      <image class="image-preview" src="{{imagePath}}" mode="widthFix"></image>
    </view>
    <view class="card">
      <text class="label">预测浓度：</text>
      <text class="value">{{concentration}}</text>
    </view>
  </view>
</scroll-view>
